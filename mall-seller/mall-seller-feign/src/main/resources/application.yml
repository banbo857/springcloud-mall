server:
  port: 9002

spring:
  application:
    name: mall-seller-feign

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka/

feign:
    hystrix:
      enabled: true